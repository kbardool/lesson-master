# Copy this file to .env and fill in your key: cp .env.example .env
# .env is gitignored — never commit real secrets.

# Required for AI-assisted features: lesson/exercise generation (app/services/ai.py)
# and subject description generation.
ANTHROPIC_API_KEY=
